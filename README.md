New project in build


This project uses Poetry
run the following:
poetry install #to install all dependencies
poetry shell #to run poetry virtual envoirnment
poetry add #add new dependency and build toml
